/*
  element_sdk.js (stub)
  โค้ดเดิมของคุณเช็ค if (window.elementSdk) ก่อนใช้งาน
  ดังนั้นบน GitHub Pages เราไม่จำเป็นต้องมี editor panel
  แต่อย่างน้อยมีไฟล์นี้ไว้เพื่อไม่ให้ 404
*/

// ปล่อยให้เป็น null เพื่อให้โค้ดหลักข้ามส่วนนี้
window.elementSdk = null;
